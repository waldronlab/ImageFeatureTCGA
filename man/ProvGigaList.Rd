% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProvGigaList.R
\docType{class}
\name{ProvGigaList}
\alias{ProvGigaList}
\alias{.ProvGigaList}
\alias{ProvGigaList-class}
\alias{path,ProvGigaList-method}
\alias{import,ProvGigaList,ANY,ANY-method}
\alias{getEmbeddings}
\title{ProvGigaList class for handling lists of ProvGiga objects}
\usage{
ProvGigaList(..., is_url = TRUE, levels = "slide_level", parallel = FALSE)

\S4method{path}{ProvGigaList}(object, ...)

\S4method{import}{ProvGigaList,ANY,ANY}(con, format, text, ...)

getEmbeddings(con, parallel = TRUE, layer = "last_layer_embed", ...)
}
\arguments{
\item{...}{Multiple \code{ProvGiga} objects, a vector of file paths or URLs, or a
list of these elements. For \code{import}, futher arguments are passed to the
\code{import} method for the individual \code{ProvGiga} objects.}

\item{object}{A \code{ProvGigaList} object.}

\item{con}{The connection from which data is loaded or to which data is
saved. If this is a \code{character} vector, it is assumed to be a file name and
a corresponding file connection is created and then closed after exporting
the object. If it is a \link[BiocIO:BiocFile-class]{BiocFile} derivative, the
data is loaded from or saved to the underlying resource. If missing, the
function will return the output as a character vector, rather than writing
to a connection.}

\item{format}{The format of the output. If missing and \code{con} is a
file name, the format is derived from the file extension. This argument is
unnecessary when \code{con} is a derivative of \link[BiocIO:BiocFile-class]{BiocFile}.}

\item{text}{If \code{con} is missing, this can be a character vector directly
providing the string data to import.}
}
\description{
The \code{ProvGigaList} class is a container for multiple \code{ProvGiga}
objects, allowing for efficient management and manipulation of collections
of ProvGiga data. It extends the \code{SimpleList} class from the \code{S4Vectors}
package.

The \code{ProvGigaList} constructor function creates an instance of
the \code{ProvGigaList} class. It accepts multiple \code{ProvGiga} objects, a vector
of file paths or URLs, or a list of these elements.

The \code{getEmbeddings} function extracts and combines the
embeddings from all \code{ProvGiga} objects within a \code{ProvGigaList}.
}
\section{\code{path}}{
 The \code{path} method for \code{ProvGigaList} objects retrieves the
file paths or URLs of all contained \code{ProvGiga} objects.
}

\section{\code{import}}{
 The \code{import} method for \code{ProvGigaList} objects imports the
data from all contained \code{ProvGiga} objects and returns a list of \code{tibbles}.
}

\examples{
slide_urls <- getCatalog("provgigapath") |>
    dplyr::filter(level == "slide_level", Project.ID == "TCGA-UVM") |>
    dplyr::slice(1:3) |>
    getFileURLs()

## set a temporary BiocFileCache cache location
old <- options(BiocFileCache.cache = tempdir())
on.exit(options(BiocFileCache.cache = old))

ProvGigaList(slide_urls) |>
   import(redownload = FALSE, parallel = FALSE)
}
