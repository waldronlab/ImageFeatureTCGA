% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HoverNet_plot.R
\name{plotHoverNetH5ADOverlay}
\alias{plotHoverNetH5ADOverlay}
\title{Plot HoverNet H5AD Segmentation Overlay with Thumbnail}
\usage{
plotHoverNetH5ADOverlay(
  hovernet,
  thumbnail_path,
  title = NULL,
  point_size = 0.01,
  legend_point_size = 2,
  color_palette = NULL,
  ncol = 2,
  rel_widths = c(1, 1.08),
  title_size = 25,
  flip_image = TRUE
)
}
\arguments{
\item{hovernet}{A \code{SpatialExperiment} or \code{SpatialFeatureExperiment} object
containing HoverNet segmentation data from h5ad format.}

\item{thumbnail_path}{Path or URL to the thumbnail PNG image file.}

\item{title}{Optional. Title for the combined plot. If \code{NULL}, uses
"HoverNet Segmentation". Default is \code{NULL}.}

\item{point_size}{Numeric value for the size of points in the segmentation
plot. Default is \code{0.01}.}

\item{legend_point_size}{Numeric value for the size of points in the legend.
Default is \code{2}.}

\item{color_palette}{Optional. A named vector of colors for cell types. If
\code{NULL}, uses RColorBrewer palette. Default is \code{NULL}.}

\item{ncol}{Number of columns for the side-by-side plots. Default is \code{2}.}

\item{rel_widths}{Relative widths of the image and segmentation plots.
Default is \code{c(1, 1.08)}.}

\item{title_size}{Font size for the main title. Default is \code{25}.}

\item{flip_image}{Logical. Whether to flip the image vertically to match
coordinate system. Default is \code{TRUE}.}
}
\value{
A \code{ggplot} object combining the thumbnail image and segmentation
overlay with an optional title.
}
\description{
Creates a side-by-side visualization of the original tissue
thumbnail image and the HoverNet cell segmentation with colored cell type
labels. This function works with HoverNet data stored in h5ad format and
imported as a SpatialExperiment object.
}
\details{
The function performs the following steps:
\enumerate{
\item Loads the thumbnail PNG image (from local path or URL with caching)
\item Creates a segmentation plot colored by cell type using the \code{type}
column from \code{colData}
\item Combines the thumbnail and segmentation side-by-side
\item Adds an optional title
}

The function expects the \code{SpatialExperiment} to have:
\itemize{
\item \code{spatialCoords} with columns \code{x_centroid} and \code{y_centroid}
\item \code{colData} with a \code{type} column for cell type classification
}

Remote PNG files are automatically cached using \code{BiocFileCache} for
efficient handling without manual downloads.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
# Load h5ad HoverNet data
h5ad_url <- paste0(
  "https://store.cancerdatasci.org/hovernet/h5ad/",
  "TCGA-VG-A8LO-01A-01-DX1.B39A4D64-82A1-4A04-8AB6-918F3058B83B.h5ad"
)
hn_spe <- HoverNet(h5ad_url, outClass = "SpatialExperiment") |> import()

thumb_url <- paste0(
  "https://store.cancerdatasci.org/hovernet/thumb/",
  "TCGA-VG-A8LO-01A-01-DX1.B39A4D64-82A1-4A04-8AB6-918F3058B83B.png"
)
plotHoverNetH5ADOverlay(hn_spe, thumbnail_path = thumb_url)
\dontshow{\}) # examplesIf}
}
