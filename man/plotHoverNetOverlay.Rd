% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/HoverNet_plot.R
\name{plotHoverNetOverlay}
\alias{plotHoverNetOverlay}
\title{Plot HoverNet Segmentation Overlay with Thumbnail}
\usage{
plotHoverNetOverlay(
  hovernet,
  json_path = NULL,
  title = NULL,
  point_size = 0.01,
  legend_point_size = 2,
  color_palette = NULL,
  ncol = 2,
  rel_widths = c(1, 1.08),
  title_size = 25,
  flip_image = TRUE
)
}
\arguments{
\item{hovernet}{A \code{SpatialExperiment} or \code{SpatialFeatureExperiment} object
created by importing a HoverNet JSON file, or a path/URL to a HoverNet
JSON file.}

\item{json_path}{Optional. Path or URL to the HoverNet JSON file. Only
required if \code{hovernet} is a \code{SpatialExperiment} object and the original
JSON path is not stored in metadata. Default is \code{NULL}.}

\item{title}{Optional. Title for the combined plot. If \code{NULL}, uses the
basename of the JSON file. Default is \code{NULL}.}

\item{point_size}{Numeric value for the size of points in the segmentation
plot. Default is \code{0.01}.}

\item{legend_point_size}{Numeric value for the size of points in the legend.
Default is \code{2}.}

\item{color_palette}{Optional. A named vector of colors for cell types. If
\code{NULL}, uses the colors from the HoverNet type map or RColorBrewer.
Default is \code{NULL}.}

\item{ncol}{Number of columns for the side-by-side plots. Default is \code{2}.}

\item{rel_widths}{Relative widths of the image and segmentation plots.
Default is \code{c(1, 1.08)}.}

\item{title_size}{Font size for the main title. Default is \code{25}.}

\item{flip_image}{Logical. Whether to flip the image vertically to match
coordinate system. Default is \code{TRUE}.}
}
\value{
A \code{ggplot} object combining the thumbnail image and segmentation
overlay with an optional title.
}
\description{
Creates a side-by-side visualization of the original tissue
thumbnail image and the HoverNet cell segmentation with colored cell type
labels. The function automatically retrieves the thumbnail image associated
with a HoverNet JSON file and overlays the segmentation data.
}
\details{
The function performs the following steps:
\enumerate{
\item If \code{hovernet} is a file path, imports it using \code{HoverNet()}
and \code{import()}
\item Retrieves the associated thumbnail PNG using \code{importHoverNetThumbnail()}
\item Creates a segmentation plot colored by cell type
\item Combines the thumbnail and segmentation side-by-side
\item Adds an optional title
}

The thumbnail is automatically downloaded and cached if a URL is provided.
The segmentation uses the \code{type} column from the \code{colData} and colors are
taken from the \code{type_map} metadata if available.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
json_url <- paste0(
  "https://store.cancerdatasci.org/hovernet/json/",
  "TCGA-VG-A8LO-01A-01-DX1.B39A4D64-82A1-4A04-8AB6-918F3058B83B.json.gz"
)
plotHoverNetOverlay(json_url, title = "TCGA-VG-A8LO Sample")
\dontshow{\}) # examplesIf}
}
